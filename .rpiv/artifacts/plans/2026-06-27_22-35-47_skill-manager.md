---
date: 2026-06-27T22:35:47+0800
author: CNife
commit: d6d0e76
branch: feature/new
repository: skill-manager
topic: "project-scoped declarative skill manager"
tags: [plan, skill-manager, python, typer, cli, mvp]
status: ready
parent: .rpiv/artifacts/discover/2026-06-25_21-29-04_project-scoped-skill-manager.md
phase_count: 5
unresolved_phase_count: 0
phases:
  - { n: 1, title: 项目骨架 + XDG 路径 }
  - { n: 2, title: 配置数据模型 + JSON 读写 + 校验 }
  - { n: 3, title: Sources/git 层 }
  - { n: 4, title: Links 层 }
  - { n: 5, title: CLI + 集成 }
last_updated: 2026-06-28T14:03:19+0800
last_updated_by: CNife
last_updated_note: "Address /check findings: path safety, ruff dependency, list status, module entrypoint"
---

# skill-manager Implementation Plan

## Overview

构建 `skill-manager`：一个纯 stdlib Python CLI（`src/skill_manager/`，uv + hatchling + typer），让每个项目通过声明式 JSON（`./.skill-manager.json`）启用自己需要的技能。`sync` 派生来源仓库、对账全局配置（`~/.config/skill-manager/config.json`）、clone/fetch 源仓库到全局缓存（`~/.cache/skill-manager/repos/`）、记录 HEAD，并在项目 `./.agents/skills/` 下为每个声明技能创建指向其目录的 symlink。`list` 读项目配置并报告来源仓库与已启用技能的状态。整体只增不删、幂等、遇外部 symlink 安全跳过。第一版只做 `sync` + `list` 闭环。

## Requirements

- `skill-manager sync` 读取当前项目 `./.skill-manager.json`：派生 sources（去重 repo）→ 写入全局配置 sources 段（若不存在）→ 确保全局缓存有 clone（缺失 clone，已有 fetch）→ 记录 HEAD 到全局配置 → 在 `./.agents/skills/` 下为每个技能创建 symlink 指向 `<repo>/<path>` 目录 → 目标已存在非本工具 symlink 时跳过不覆盖 → 源仓库中找不到指定路径时报错退出非 0 → 幂等
- `skill-manager list` 读取当前项目 `./.skill-manager.json` 并输出：来源仓库列表、启用技能列表（repo + path + name），每个技能标注仓库目录是否存在、symlink 状态（`linked`/`external`/`broken`/`unlinked`）
- 全局配置文件缺省目录时自动创建
- `./.skill-manager.json` 格式错误时 `sync` 报错退出非 0
- JSON 中指定不存在的仓库路径时 `sync` 报错退出非 0
- 实现语言 Python + typer，uv 管理依赖与运行环境，ruff 格式化/检查，pytest 测试，pre-commit 钩子
- 命令名 `skill-manager`（不接受缩写 `sk`）；GitHub 远端仅 `owner/repo`，工具推导为 `https://github.com/owner/repo.git`；repo 两段与 path 均拒绝路径穿越（`.`/`..`/空段/分隔符逃逸）

## Current State Analysis

代码库基线为空仓库（`/home/cnife/code/skill-manager`），仅有 `README.md`（一行标题）、`.gitignore`（内容 `.pi/`）与 FRD artifact。无既有代码可复用，属全新项目。

### Key Discoveries

- `~/.agents/.skill-lock.json`（Vercel skills v3 lock）：真实技能身份形状参考 —— 每个技能记录 `source`（owner/repo）、`sourceType: github`、`sourceUrl`、`skillPath`（相对 SKILL.md 的路径，如 `skills/read/SKILL.md` 或根目录 `SKILL.md`）、`skillFolderHash`、`installedAt`/`updatedAt`。本工具的 `name + repo + path` 身份与之同源
- `~/.pi/agent/skills/` 下全局技能是 **symlink**，指向 `../../../.agents/skills/<name>`（真实目录）。即 pi 通过目录名加载技能，symlink 目标是含 `SKILL.md` 的目录。本工具项目级 `./.agents/skills/` 采用同款形状：symlink → 源仓库内技能目录
- 技能目录形状：`SKILL.md` + 可选子目录（`references/`、`scripts/`）。故 symlink 必须指向目录而非 `SKILL.md` 文件
- `~/.agents/skills/` 现含 23 个真实目录技能（read/check/hunt/learn/kami/ast-grep 等），其中 `kami` 是 repo 根目录技能（`source: tw93/Kami`，`skillPath: SKILL.md`）—— 证实 repo 根目录技能是真实用例，必须支持
- 工程骨架参照 `/home/cnife/code/my-scripts`：typer + rich、dev 组含 pre-commit/pytest/pytest-cov/pytest-timeout、ruff lint（`extend-select = ["B","C4","I","PIE","RUF","S","SIM","UP","W"]`，忽略 `S607/S101` 等）、pytest `timeout=30` + `network` marker、`.pre-commit-config.yaml`（pre-commit-hooks + uv-lock + ruff-format + ruff-check）。本工具改用 hatchling + src 布局

### Patterns to follow

- 工程骨架：`my-scripts/pyproject.toml` + `.pre-commit-config.yaml`（typer/ruff/pre-commit/pytest 配置范式），改 hatchling build-backend + `[project.scripts]` console_scripts
- 技能身份与 symlink 形状：`~/.agents/.skill-lock.json` + `~/.pi/agent/skills/` symlink 模式

### Constraints to work within

- Python 3.11+（FRD 约束；本计划用 3.12 作 target-version 以对齐 my-scripts 风格，requires-python 设 `>=3.11`）
- 不引入数据库/服务；仅配置读写 + git clone/fetch + symlink
- 仅 GitHub `owner/repo`；非 GitHub 远端延后
- 不监听目录切换（无 Pi 扩展）；不竞争 Vercel `skills` 安装机制
- 安全默认：非本工具 symlink 跳过不覆盖；缺失/冲突 → 非零退出

## Desired End State

项目根有 `./.skill-manager.json`：

```json
{
  "skills": [
    {"name": "read", "repo": "tw93/Waza", "path": "skills/read"},
    {"name": "kami", "repo": "tw93/Kami", "path": "."}
  ]
}
```

终端用法：

```bash
$ skill-manager sync
cloned tw93/Waza -> ~/.cache/skill-manager/repos/tw93/Waza (HEAD abc1234)
fetched tw93/Kami (HEAD def5678)
linked read -> ~/.cache/skill-manager/repos/tw93/Waza/skills/read
linked kami -> ~/.cache/skill-manager/repos/tw93/Kami

$ skill-manager list
Sources:
  tw93/Waza   abc1234  (cached)
  tw93/Kami   def5678  (cached)

Skills:
  read   tw93/Waza:skills/read   linked (valid)
  kami   tw93/Kami:.              linked (valid)

$ ls -l ./.agents/skills/
read -> ~/.cache/skill-manager/repos/tw93/Waza/skills/read
kami -> ~/.cache/skill-manager/repos/tw93/Kami

$ skill-manager sync   # 再次执行，幂等，无副作用，退出 0
```

错误用法：

```bash
$ skill-manager sync   # .skill-manager.json 格式错误
Error: invalid .skill-manager.json: <reason>
$ echo $?
1

$ skill-manager sync   # path 在仓库中不存在
Error: skill "read" path "skills/missing" not found in tw93/Waza
$ echo $?
1
```

## What We're NOT Doing

- 不做 `enable/disable` 编辑命令（用户直接写 JSON）
- 不做 `check` 健康命令（断链/缺 SKILL.md 检测）—— FRD Suggested Follow-ups，延后
- 不做交互式勾选 UI（curses/TUI）
- 不实现数据库、HTTP 服务器、GUI、daemon
- 不实现 Pi 扩展（不监听目录切换事件）
- 不竞争或取代 Vercel `skills` 安装机制
- 不支持非 GitHub 远端（第一版只认 `owner/repo`）
- **sync 不做清理**：不删除 `./.agents/skills/` 下未声明的 symlink（只增不删；移除由用户手动）
- 不维护全局「已启用技能」注册表（FRD 完整工具第 3 层，MVP 不做；全局 config 只存 sources）
- 不做跨 sources 同名目录冲突的专门检测命令（重名在配置校验阶段即报错）
- 不触碰 `my-scripts` 等参考项目源码（只读参照）

## Decisions

### Ambiguity — sources 声明方式
**Explored**: (A) 从 skills 派生 sources —— 只声明 skills，sources 取去重后的 repo，DRY，MVP 足够；(B) 显式 sources 列表 —— 同时声明 sources 与 skills，可声明仓库但不启用技能（MVP 用不到），需手动保持两边一致。
**Decision**: A — 从 skills 派生。证据：FRD MVP sync 第 1 条「把项目声明的 owner/repo 写入全局配置 sources 段」可由 skills 的 repo 字段自动得出；MVP 无「声明仓库但不启用技能」用例。

### Ambiguity — path 语义
**Explored**: (A) path 存技能目录（如 `skills/read`），工具拼接 `/SKILL.md` 校验存在 —— 贴近 FRD「SKILL.md 所在目录」表述，symlink 直接指向该目录；(B) path 存 SKILL.md 文件路径（如 `skills/read/SKILL.md`），与 Vercel `~/.agents/.skill-lock.json` 的 `skillPath` 一致、跨工具互操作更友好，symlink 目标取父目录。
**Decision**: A — path 存技能目录。证据：FRD 明确「指向源仓库内对应路径的 SKILL.md 所在目录」；symlink 目标即 `<repo>/<path>`，无须再取父目录，语义最直接。repo 根目录技能用 `path: "."`。

### Ambiguity — symlink 命名
**Explored**: (A) 派生：名字取 path 目录末段（`skills/foo` → `foo`），repo 根目录技能取 repo 末段并小写（`tw93/Kami` → `kami`），无别名字段、忠于 FRD 身份决策；(B) 显式 name 字段 —— 每个技能声明 name，symlink 用它命名，repo 根目录技能与重名控制更清晰，但重新引入 FRD 明确拒绝的别名管理。
**Decision**: B — 显式 name 字段（开发者选定，覆盖 A 推荐）。此决策**细化**而非否定 FRD 的「repo + path」身份决策：config 身份升级为 `name + repo + path`，`name` 作为磁盘 symlink 句柄。约束：`name` 校验为安全 basename（非空、无路径分隔符、无 `.`/`..`）；配置内重名 → 校验报错退出非 0。证据：repo 根目录技能（kami）真实存在，显式 name 避免派生歧义。

### Ambiguity — sync 是否清理
**Explored**: (A) 只增不删 —— sync 只确保声明技能存在，不删任何已有 symlink，最安全、符合「非本工具 symlink 跳过」；(B) 清理未声明的本工具链接 —— 删除本工具创建且不在当前配置的 symlink，真正「恢复完整状态」，但需额外元数据标记本工具创建的链接，复杂度上升。
**Decision**: A — 只增不删。证据：FRD 安全默认「目标目录已存在非本工具创建的 symlink 时跳过而非覆盖」；MVP 不维护本工具创建标记；移除技能由用户手动删链接（或后续 enable/disable 命令处理）。

### Simple — 全局配置 sources 结构
**Decision**: sources 用 dict 按 repo 索引：`{"sources": {"owner/repo": {"commit": "<HEAD>", "url": "https://github.com/owner/repo.git"}}}`。dict 便于「若不存在」判断与 commit 字段就地更新。证据：FRD MVP sync「写入全局配置 sources 段（若不存在）」「将仓库 HEAD 写入全局配置 sources 段的 commit 字段」。

### Simple — 内部类型
**Decision**: 用 stdlib `dataclasses` 定义 `SkillRef`（name/repo/path）、`ProjectConfig`（skills 列表）、`Source`（repo/commit/url）、`GlobalConfig`（sources dict）。无 pydantic，保持 stdlib 依赖面最小。

### Simple — 模块拆分
**Decision**: `src/skill_manager/` 下分 `paths.py`（XDG 路径解析）、`config.py`（dataclasses + JSON 读写 + 校验）、`sources.py`（ensure_source：clone/fetch/HEAD + 全局配置更新）、`links.py`（ensure_link：symlink 创建/校验/跳过）、`cli.py`（typer app + sync/list）。每层单一职责，便于单测。

### Simple — 测试策略
**Decision**: pytest + `tmp_path`；用 `git init --bare` 本地仓库 + `file://` URL 作离线 GitHub 替身（确定性、无网络）；真连网冒烟用 `@pytest.mark.network` marker（默认 `-m "not network"`）。subprocess 调用 git 用 list 形参、不开 shell（ruff `S603`/`S607` 在 my-scripts 已忽略，本工具沿用并加注释说明安全约束）。

### Simple — 工程骨架
**Decision**: hatchling build-backend + src 布局 + `[project.scripts] skill-manager = "skill_manager.cli:app"`；ruff 配置对齐 my-scripts；pre-commit 钩子同 my-scripts。证据：FRD「src 布局 + hatchling」「ruff + pre-commit 钩子」。

## Ordering Constraints

- Phase 1（骨架 + 路径）必须最先：定义包结构、构建配置、XDG 路径解析，后续所有模块依赖
- Phase 2（config）依赖 Phase 1 的 paths（定位配置文件）
- Phase 3（sources）依赖 Phase 2 的 config（读写全局配置、SkillRef 类型）；conftest.py 在此 phase 引入，供 Phase 5 复用
- Phase 4（links）依赖 Phase 1 的 paths 与 Phase 2 的 config 类型；与 Phase 3 无依赖，但按序排列
- Phase 5（CLI + 集成）依赖 Phase 2/3/4，串起完整闭环；README 用法文档随此 phase 落地
- 严格顺序执行：1 → 2 → 3 → 4 → 5

## Verification Notes

来源：FRD 验收标准 + 风险行为。Step 6.1 据此细化为各 phase 的 Success Criteria。

- sync 幂等：重复执行不产生额外 clone 或 symlink 重复创建，退出 0 —— 端到端在 Phase 5 验证；ensure_source/ensure_link 的幂等分别在 Phase 3/4 单测
- 非本工具 symlink 跳过：`./.agents/skills/` 下已有指向他处的 symlink 时，sync 跳过不覆盖 —— Phase 4 `test_links.py` 覆盖；Phase 5 端到端复验
- 源仓库中找不到指定 path：报错退出非 0 —— Phase 4 links 层校验 `<repo>/<path>` 与 `SKILL.md`；Phase 5 端到端复验
- repo/path 路径安全：repo 两段必须是安全 GitHub slug；path 必须是 repo 内相对 POSIX 目录，拒绝 `..`/空段/绝对路径/反斜杠 —— Phase 2 `test_config.py` + Phase 4 defense-in-depth 覆盖
- `./.skill-manager.json` 格式错误：报错退出非 0 —— Phase 2 `test_config.py` 覆盖（坏 JSON、缺字段、重名、非法 name）
- HEAD 写入全局配置 sources：sync 后 `config.json` 的 sources[repo].commit 等于仓库当前 HEAD —— Phase 3 `test_sources.py`
- symlink 有效：sync 后 `ls -l ./.agents/skills/` 每个技能是有效 symlink —— Phase 4 + Phase 5
- list 输出 sources 与技能状态（仓库目录是否存在、symlink 状态；外部 symlink 必须报 `external` 而非误报 `linked`）—— Phase 5 `test_cli.py`
- XDG 环境变量尊重：`XDG_CONFIG_HOME` / `XDG_CACHE_HOME` 覆盖默认路径 —— Phase 1 `test`（纳入 Phase 2 或独立断言）+ Phase 3/5 间接验证
- subprocess 安全：git 调用用 list 形参、不开 shell —— 代码审查 + ruff 通过
- 全局配置缺省目录自动创建 —— Phase 3（首次写全局配置前确保目录）

## Performance Considerations

无明显性能要求。sync 受 git clone/fetch 速度支配，属可接受范围。fetch 增量、仅按需执行；config 为小 JSON，O(skills) 遍历，无 N+1。HEAD 记录避免重复 fetch 判断（但 MVP sync 每次 fetch 仓库以更新 HEAD，fetch 本身廉价）。sources 按 repo 去重，同 repo 多技能只 clone/fetch 一次。

## Migration Notes

不适用 —— 全新项目，无既有持久化 schema 需迁移。全局配置与项目配置均为新建文件；首次运行自动创建缺省目录与文件。

## Pattern References

- `/home/cnife/code/my-scripts/pyproject.toml` —— typer/ruff/pytest/pre-commit 配置范式（dev 组、ruff lint extend-select、pytest timeout+network marker）
- `/home/cnife/code/my-scripts/.pre-commit-config.yaml` —— pre-commit hooks 组合（pre-commit-hooks + uv-lock + ruff-format + ruff-check）
- `~/.agents/.skill-lock.json` —— 技能身份形状（source/skillPath/installedAt），`name+repo+path` 同源参考
- `~/.pi/agent/skills/` —— symlink 指向含 SKILL.md 目录的加载模式

## Developer Context

### Step 4 检查点问答（含 file:line 证据）

- **Q1 Sources 声明**：项目配置里 sources 怎么声明？参考 FRD MVP sync 第 1 条「把项目声明的 owner/repo 写入全局配置 sources 段」。
  - 选定：**从 skills 派生** —— `{skills:[{repo,path}]}`，sources 自动取去重 repo。DRY，MVP 足够。
- **Q2 Path 语义**：path 存技能目录还是 SKILL.md 文件路径？FRD「symlink 指向 SKILL.md 所在目录」；Vercel `~/.agents/.skill-lock.json` 用 SKILL.md 文件路径（`skills/read/SKILL.md`）。
  - 选定：**技能目录路径** —— `path=skills/read`，工具拼接 `/SKILL.md` 校验；symlink 目标即 `<repo>/<path>`。
- **Q3 链接命名**：symlink 名字怎么定？FRD 身份决策选 repo+path 拒绝 alias，但 repo 根目录技能（kami，`~/.agents/.skill-lock.json` 中 `source: tw93/Kami, skillPath: SKILL.md`）派生名字会落到仓库名。
  - 选定：**显式 name 字段**（覆盖推荐的派生方案）—— 每技能声明 name，symlink 用 name 命名；name 校验为安全 basename，配置内重名报错。此为对 FRD 身份决策的细化（config 身份 = name+repo+path）。
- **Q4 Sync 清理**：sync 是否删除 `./.agents/skills/` 下未声明的 symlink？FRD「恢复完整状态 + 幂等」对「非本工具 symlink 跳过不覆盖」。
  - 选定：**只增不删** —— sync 只确保声明技能存在，不删任何已有 symlink；移除由用户手动。

### Step 4 设计摘要确认
- 选定：**Proceed** —— 进入切片分解。

### Step 5 切片分解确认
- 选定：**Approve** —— 5 个顺序切片（骨架/路径 → config → sources/git → links → CLI/集成）。

### Step 6 逐片生成与微检查（slice-verifier 失效说明）
- slice-verifier 子代理在本环境失效：3 次派发（Phase 1 两次、Phase 2 一次）均完成执行但无最终输出且未落盘 `/tmp/sv_phase*.md`（`get_subagent_result` 返回 "Agent not found"，`cat` 确认文件未写入）。
- 缓解措施（三层兜底）：(1) 每片 6.1 后由主 agent 做严格自检（抓到 Phase 4 `test_links.py` 缺 `import os` 真实 bug 并修复、Phase 2 去掉未用的 `save_project_config`/`import os`）；(2) 每片 6.3 经开发者 `ask_user_question` 批准；(3) Step 8 强制独立双评审（artifact-code-reviewer + artifact-coverage-reviewer 并行）补上独立审计。流程完整性未受影响——SLF 要求的独立验证由 Step 8 兜底。
- 各片批准记录见 Plan History。生成期发现的偏离计划细化：Phase 1 加 cli.py 桩、Phase 3 改 `pull --ff-only` + path 校验移至 Phase 4 + 不重定义 `repo_cache_path`、Phase 4 用绝对 symlink 目标，均已写入对应 phase 文本。

## Plan History

- Phase 1: 项目骨架 + XDG 路径 — approved as generated
- Phase 2: 配置数据模型 + JSON 读写 + 校验 — approved as generated
- Phase 3: Sources/git 层 — approved as generated
- Phase 4: Links 层 — approved as generated
- Phase 5: CLI + 集成 — approved as generated

## Plan Review (Step 8)

_Independent post-finalization review by artifact-code-reviewer and artifact-coverage-reviewer subagents. Findings triaged at Step 9._

| source   | plan-loc          | codebase-loc                | severity   | dimension             | finding   | recommendation   | resolution         |
| -------- | ----------------- | --------------------------- | ---------- | --------------------- | --------- | ---------------- | ------------------ |
| coverage | (all)             | <n/a>                       | ok         | verification-coverage | All 10 Verification Notes covered (both criteria + code-mirror paths) | — | covered: no action needed |
| code     | (all)             | (all)                       | (unreviewed) | (all)               | _Step 8 code review failed: artifact-code-reviewer completed execution but returned no capturable output and wrote no findings file (same environment issue as slice-verifier). Mitigated: main-agent self-audit (tokenize all 12 Python blocks clean, no missing imports, cross-module symbol resolution all valid, module decoupling invariants hold, code copy-pasteable). | Re-run code review in a session where subagent output capture works, or rely on implement-time ruff/pytest + Step 9 developer review. | unreviewed: self-audit passed, developer review at Step 9 |

### Post-check Revision (2026-06-28)

- Incorporated `/check` findings: repo component validation hardened, skill path traversal rejected, `list` now distinguishes external/broken symlinks from declared valid links, `ruff` added to dev dependencies, and `src/skill_manager/__main__.py` added for `python -m skill_manager`.
- Newly required tests: unsafe repo/path config rejection, links-layer repo-boundary defense, and `run_list` external-symlink reporting.

## References

- `.rpiv/artifacts/discover/2026-06-25_21-29-04_project-scoped-skill-manager.md` —— 上游 FRD（决策、需求、验收标准）
- `~/.agents/.skill-lock.json` —— Vercel skills v3 lock，技能身份形状参考
- `/home/cnife/code/my-scripts/pyproject.toml`、`.pre-commit-config.yaml` —— 工程骨架范式

---

## Phase 1: 项目骨架 + XDG 路径

### Overview

奠定工程基座：pyproject.toml（hatchling + src 布局 + typer 依赖 + console_scripts）、pre-commit 钩子、.gitignore 补充、包入口 `__init__.py`、包级执行入口 `__main__.py`、`paths.py`（XDG 配置/缓存路径解析）、`cli.py` 桩（空 Typer app，供 `--help`、`python -m skill_manager --help` 与 console_scripts 入口解析；sync/list 命令在 Phase 5 经 MODIFY 加入）。无前置依赖；后续所有模块依赖此 phase 的包结构与路径解析。

### Changes Required:

#### 1. pyproject.toml
**File**: pyproject.toml
**Changes**: NEW — uv/hatchling 项目定义、typer 依赖、ruff/pytest/pre-commit dev 组、`[project.scripts] skill-manager` 入口、ruff lint 配置、pytest 配置

```toml
[project]
name = "skill-manager"
version = "0.1.0"
description = "Project-scoped declarative skill manager for pi agent skills"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "typer>=0.12",
]

[project.scripts]
skill-manager = "skill_manager.cli:app"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/skill_manager"]

[dependency-groups]
dev = [
    "pre-commit",
    "pytest>=8",
    "pytest-cov>=5",
    "pytest-timeout>=2",
    "ruff>=0.14",
]

[tool.pytest.ini_options]
timeout = 30
testpaths = ["tests"]
markers = [
    "network: marks tests as requiring network access (deselect with '-m \"not network\"')",
]

[tool.ruff]
target-version = "py311"
line-length = 100

[tool.ruff.lint]
extend-select = ["B", "C4", "I", "PIE", "RUF", "S", "SIM", "UP", "W"]
ignore = ["S101", "S603", "S607"]

[tool.ruff.lint.per-file-ignores]
"tests/**" = ["S"]
```

#### 2. .pre-commit-config.yaml
**File**: .pre-commit-config.yaml
**Changes**: NEW — pre-commit hooks（pre-commit-hooks + uv-lock + ruff-format + ruff-check），对齐 my-scripts

```yaml
repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: mixed-line-ending
        args: ["--fix=lf"]
      - id: check-json
      - id: check-toml
      - id: check-yaml
  - repo: https://github.com/astral-sh/uv-pre-commit
    rev: "0.9.22"
    hooks:
      - id: uv-lock
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.14.10
    hooks:
      - id: ruff-format
      - id: ruff-check
        args: ["--fix"]
```

#### 3. .gitignore
**File**: .gitignore
**Changes**: MODIFY — 在现有 `.pi/` 基础上补充 Python/uv 构建产物忽略；另忽略 `./.agents/skills/`（sync 生成的绝对路径 symlink，机器本地、不应提交；原文件仅一行 `.pi/`，此为完整新内容；保留 `.pi/` 忽略，不忽略 `.rpiv/` 以提交 artifacts）

```gitignore
.pi/
.agents/skills/
# Python / uv
__pycache__/
*.py[cod]
.venv/
dist/
build/
*.egg-info/
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/
```

#### 4. src/skill_manager/__init__.py
**File**: src/skill_manager/__init__.py
**Changes**: NEW — 包入口，导出 `__version__`

```python
"""skill-manager: project-scoped declarative skill manager."""

__version__ = "0.1.0"
```

#### 5. src/skill_manager/paths.py
**File**: src/skill_manager/paths.py
**Changes**: NEW — XDG 路径解析：全局配置文件路径、全局源缓存根目录、单个 repo 缓存路径、项目配置路径、项目技能链接目录

```python
"""XDG-aware path resolution for skill-manager.

All path functions are pure: they compute locations without creating them.
Directory creation is the caller's responsibility (config/sources layers).
"""

from __future__ import annotations

import os
from pathlib import Path

APP_NAME = "skill-manager"


def _xdg(env_var: str, default_subdir: str) -> Path:
    """Resolve an XDG base directory, falling back to $HOME/<default_subdir>."""
    value = os.environ.get(env_var)
    if value:
        return Path(value)
    return Path.home() / default_subdir


def config_dir() -> Path:
    """Global config directory: $XDG_CONFIG_HOME/skill-manager (default ~/.config/skill-manager)."""
    return _xdg("XDG_CONFIG_HOME", ".config") / APP_NAME


def config_file() -> Path:
    """Global config file path."""
    return config_dir() / "config.json"


def cache_dir() -> Path:
    """Global cache directory: $XDG_CACHE_HOME/skill-manager (default ~/.cache/skill-manager)."""
    return _xdg("XDG_CACHE_HOME", ".cache") / APP_NAME


def repos_cache_dir() -> Path:
    """Root of cloned source repositories."""
    return cache_dir() / "repos"


def repo_cache_path(repo: str) -> Path:
    """Path of a cloned repo inside the cache.

    ``repo`` is ``owner/repo``; the slash maps to a nested directory so distinct
    owners never collide.
    """
    return repos_cache_dir() / repo


def project_config_path(project_dir: Path | None = None) -> Path:
    """Path to the project's ``.skill-manager.json``."""
    base = Path(project_dir) if project_dir is not None else Path.cwd()
    return base / ".skill-manager.json"


def project_skills_dir(project_dir: Path | None = None) -> Path:
    """Path to the project's skill link directory (``./.agents/skills/``)."""
    base = Path(project_dir) if project_dir is not None else Path.cwd()
    return base / ".agents" / "skills"
```

#### 6. src/skill_manager/cli.py
**File**: src/skill_manager/cli.py
**Changes**: NEW — Typer app 桩（空 app，无子命令），供 console_scripts 入口解析与 `--help`；sync/list 命令在 Phase 5 经 MODIFY 加入

```python
"""skill-manager CLI entry point.

Commands (sync, list) are added in Phase 5; this module provides the Typer app
and the console_scripts target so ``skill-manager --help`` works once installed.
"""

from __future__ import annotations

import typer

app = typer.Typer(
    name="skill-manager",
    help="Project-scoped declarative skill manager for pi agent skills.",
    no_args_is_help=True,
)


def main() -> None:
    """Entry point for ``python -m skill_manager``."""
    app()


if __name__ == "__main__":
    main()
```

#### 7. src/skill_manager/__main__.py
**File**: src/skill_manager/__main__.py
**Changes**: NEW — 包级执行入口，让 `python -m skill_manager` 调用同一个 Typer app

```python
"""Package execution entry point for ``python -m skill_manager``."""

from skill_manager.cli import main

if __name__ == "__main__":
    main()
```

### Success Criteria:

#### Automated Verification:
- [x] `uv sync` 成功安装依赖并创建 .venv：`uv sync`
- [x] 包可导入：`uv run python -c "import skill_manager; from skill_manager import paths, cli; print(skill_manager.__version__)"`
- [x] `skill-manager --help` 退出 0：`uv run skill-manager --help`
- [x] 包级执行入口退出 0：`uv run python -m skill_manager --help`
- [x] ruff 检查通过：`uv run ruff check .`
- [x] ruff 格式检查通过：`uv run ruff format --check .`
- [x] pre-commit 钩子通过（首次安装后）：`uv run pre-commit run --all-files`

#### Manual Verification:
- [ ] `paths.config_file()` 在 `XDG_CONFIG_HOME=/tmp/x` 时返回 `/tmp/x/skill-manager/config.json`，未设置时回退 `~/.config/skill-manager/config.json`
- [ ] `paths.cache_dir()` 在 `XDG_CACHE_HOME=/tmp/x` 时返回 `/tmp/x/skill-manager`，未设置时回退 `~/.cache/skill-manager`
- [ ] `paths.repo_cache_path("tw93/Waza")` 返回 `<cache>/repos/tw93/Waza`
- [ ] 项目为 `src/skill_manager/` 布局，`pyproject.toml` 含 `[project.scripts] skill-manager = "skill_manager.cli:app"`

---

## Phase 2: 配置数据模型 + JSON 读写 + 校验

### Overview

定义 dataclasses（`SkillRef`/`ProjectConfig`/`Source`/`GlobalConfig`）与 JSON 加载/保存/校验逻辑，覆盖项目配置（`./.skill-manager.json`）与全局配置（`~/.config/skill-manager/config.json`）。校验：name 安全 basename、配置内重名、path 目录语义。依赖 Phase 1 的 paths 定位配置文件。

### Changes Required:

#### 1. src/skill_manager/config.py
**File**: src/skill_manager/config.py
**Changes**: NEW — dataclasses + JSON load/save + 校验（重名/非法 name/缺字段/非法 repo/绝对 path）+ 错误类型；纯 I/O+校验，不 import paths，不包含 app 不用的 save_project_config

```python
"""Config data model, JSON I/O, and validation for skill-manager.

Pure I/O + validation: functions take explicit Path arguments and do not import
paths.py. Callers (sources/cli layers) resolve locations via paths.* and pass them in.
"""

from __future__ import annotations

import json
from dataclasses import dataclass, field
from pathlib import Path, PurePosixPath


class ConfigError(Exception):
    """Raised when a config file is missing, malformed, or invalid."""


@dataclass(frozen=True)
class SkillRef:
    name: str
    repo: str
    path: str


@dataclass
class ProjectConfig:
    skills: list[SkillRef]


@dataclass(frozen=True)
class Source:
    repo: str
    commit: str
    url: str


@dataclass
class GlobalConfig:
    sources: dict[str, Source] = field(default_factory=dict)


def derived_sources(config: ProjectConfig) -> list[str]:
    """Unique repo identifiers from a project config, in first-seen order."""
    seen: set[str] = set()
    repos: list[str] = []
    for skill in config.skills:
        if skill.repo not in seen:
            seen.add(skill.repo)
            repos.append(skill.repo)
    return repos


def _validate_name(name: str, path: Path) -> None:
    if not name or PurePosixPath(name).name != name or name in (".", ".."):
        raise ConfigError(f"skill name {name!r} must be a single path component in {path}")


def _is_safe_repo_component(value: str) -> bool:
    """True when value is a single GitHub-like slug component, not a path."""
    return (
        bool(value)
        and value not in (".", "..")
        and "\\" not in value
        and PurePosixPath(value).name == value
        and all(c.isascii() and (c.isalnum() or c in "._-") for c in value)
    )


def _validate_repo(repo: str, name: str, path: Path) -> None:
    parts = repo.split("/")
    if len(parts) != 2 or not all(_is_safe_repo_component(part) for part in parts):
        raise ConfigError(f"skill {name!r} repo {repo!r} must be safe 'owner/repo' in {path}")


def _validate_path(skill_path: str, name: str, path: Path) -> None:
    if skill_path == ".":
        return
    segments = skill_path.split("/")
    if (
        PurePosixPath(skill_path).is_absolute()
        or "\\" in skill_path
        or any(segment in ("", ".", "..") for segment in segments)
    ):
        raise ConfigError(
            f"skill {name!r} path {skill_path!r} must be a relative repo-internal directory in {path}"
        )


def _parse_skill(item: object, path: Path) -> SkillRef:
    if not isinstance(item, dict):
        raise ConfigError(f"each skill must be an object in {path}, got {type(item).__name__}")
    name = item.get("name")
    repo = item.get("repo")
    skill_path = item.get("path")
    if not isinstance(name, str) or not name:
        raise ConfigError(f"skill entry missing 'name' in {path}: {item!r}")
    if not isinstance(repo, str) or not repo:
        raise ConfigError(f"skill {name!r} missing 'repo' in {path}")
    if not isinstance(skill_path, str) or not skill_path:
        raise ConfigError(f"skill {name!r} missing 'path' in {path}")
    _validate_name(name, path)
    _validate_repo(repo, name, path)
    _validate_path(skill_path, name, path)
    return SkillRef(name=name, repo=repo, path=skill_path)


def _check_duplicate_names(skills: list[SkillRef], path: Path) -> None:
    seen: set[str] = set()
    for skill in skills:
        if skill.name in seen:
            raise ConfigError(f"duplicate skill name {skill.name!r} in {path}")
        seen.add(skill.name)


def load_project_config(path: Path) -> ProjectConfig:
    if not path.is_file():
        raise ConfigError(f"project config not found: {path}")
    try:
        data = json.loads(path.read_text(encoding="utf-8"))
    except json.JSONDecodeError as e:
        raise ConfigError(f"invalid JSON in {path}: {e}") from e
    if not isinstance(data, dict):
        raise ConfigError(f"project config must be a JSON object in {path}, got {type(data).__name__}")
    skills_raw = data.get("skills")
    if not isinstance(skills_raw, list):
        raise ConfigError(f"project config missing 'skills' list in {path}")
    skills = [_parse_skill(item, path) for item in skills_raw]
    _check_duplicate_names(skills, path)
    return ProjectConfig(skills=skills)


def load_global_config(path: Path) -> GlobalConfig:
    if not path.is_file():
        return GlobalConfig()
    try:
        data = json.loads(path.read_text(encoding="utf-8"))
    except json.JSONDecodeError as e:
        raise ConfigError(f"invalid JSON in {path}: {e}") from e
    if not isinstance(data, dict):
        raise ConfigError(f"global config must be a JSON object in {path}, got {type(data).__name__}")
    sources_raw = data.get("sources", {})
    if not isinstance(sources_raw, dict):
        raise ConfigError(f"global config 'sources' must be an object in {path}")
    sources: dict[str, Source] = {}
    for repo, entry in sources_raw.items():
        if not isinstance(entry, dict):
            raise ConfigError(f"global config source {repo!r} must be an object in {path}")
        commit = entry.get("commit", "")
        url = entry.get("url", "")
        if not isinstance(commit, str) or not isinstance(url, str):
            raise ConfigError(f"global config source {repo!r} has non-string fields in {path}")
        sources[repo] = Source(repo=repo, commit=commit, url=url)
    return GlobalConfig(sources=sources)


def save_global_config(path: Path, config: GlobalConfig) -> None:
    path.parent.mkdir(parents=True, exist_ok=True)
    data = {
        "sources": {
            repo: {"commit": src.commit, "url": src.url} for repo, src in config.sources.items()
        }
    }
    path.write_text(json.dumps(data, indent=2, sort_keys=True) + "\n", encoding="utf-8")
```

#### 2. tests/test_config.py
**File**: tests/test_config.py
**Changes**: NEW — 配置加载/保存/校验单测：合法配置（含 path="."）、坏 JSON、非 object、缺 skills、缺 name/repo/path、非法 name、非法 repo（含路径逃逸）、非法 path（含路径逃逸）、重名、derived_sources 去重保序、全局配置缺失→空、全局配置往返（建父目录）

```python
import json
from pathlib import Path

import pytest

from skill_manager.config import (
    ConfigError,
    GlobalConfig,
    ProjectConfig,
    SkillRef,
    Source,
    derived_sources,
    load_global_config,
    load_project_config,
    save_global_config,
)


def _write(path: Path, data: object) -> None:
    path.write_text(json.dumps(data), encoding="utf-8")


def test_load_project_config_valid(tmp_path: Path) -> None:
    p = tmp_path / ".skill-manager.json"
    _write(p, {"skills": [
        {"name": "read", "repo": "tw93/Waza", "path": "skills/read"},
        {"name": "kami", "repo": "tw93/Kami", "path": "."},
    ]})
    cfg = load_project_config(p)
    assert cfg.skills == [
        SkillRef("read", "tw93/Waza", "skills/read"),
        SkillRef("kami", "tw93/Kami", "."),
    ]


def test_load_project_config_missing(tmp_path: Path) -> None:
    with pytest.raises(ConfigError, match="not found"):
        load_project_config(tmp_path / ".skill-manager.json")


def test_load_project_config_bad_json(tmp_path: Path) -> None:
    p = tmp_path / ".skill-manager.json"
    p.write_text("{not valid", encoding="utf-8")
    with pytest.raises(ConfigError, match="invalid JSON"):
        load_project_config(p)


def test_load_project_config_not_object(tmp_path: Path) -> None:
    p = tmp_path / ".skill-manager.json"
    _write(p, ["a"])
    with pytest.raises(ConfigError, match="JSON object"):
        load_project_config(p)


def test_load_project_config_missing_skills(tmp_path: Path) -> None:
    p = tmp_path / ".skill-manager.json"
    _write(p, {"foo": 1})
    with pytest.raises(ConfigError, match="skills"):
        load_project_config(p)


@pytest.mark.parametrize("missing", ["name", "repo", "path"])
def test_skill_missing_field(tmp_path: Path, missing: str) -> None:
    entry = {"name": "read", "repo": "tw93/Waza", "path": "skills/read"}
    entry.pop(missing)
    p = tmp_path / ".skill-manager.json"
    _write(p, {"skills": [entry]})
    with pytest.raises(ConfigError, match=missing):
        load_project_config(p)


@pytest.mark.parametrize("bad", [".", "..", "a/b", "a/", "/a", ""])
def test_invalid_name(tmp_path: Path, bad: str) -> None:
    p = tmp_path / ".skill-manager.json"
    _write(p, {"skills": [{"name": bad, "repo": "tw93/Waza", "path": "skills/read"}]})
    with pytest.raises(ConfigError, match="name"):
        load_project_config(p)


@pytest.mark.parametrize(
    "bad",
    [
        "tw93",
        "tw93/Waza/x",
        "/Waza",
        "tw93/",
        "",
        "../evil",
        "owner/..",
        "owner/.",
        "./repo",
        "owner/r\\x",
        "owner/repo with space",
    ],
)
def test_invalid_repo(tmp_path: Path, bad: str) -> None:
    p = tmp_path / ".skill-manager.json"
    _write(p, {"skills": [{"name": "read", "repo": bad, "path": "skills/read"}]})
    with pytest.raises(ConfigError, match="repo"):
        load_project_config(p)


@pytest.mark.parametrize(
    "bad",
    [
        "/abs/read",
        "../outside",
        "skills/../read",
        "skills//read",
        "skills/read/",
        "skills/./read",
        "skills\\read",
    ],
)
def test_invalid_path_rejected(tmp_path: Path, bad: str) -> None:
    p = tmp_path / ".skill-manager.json"
    _write(p, {"skills": [{"name": "read", "repo": "tw93/Waza", "path": bad}]})
    with pytest.raises(ConfigError, match="repo-internal"):
        load_project_config(p)


def test_duplicate_names(tmp_path: Path) -> None:
    p = tmp_path / ".skill-manager.json"
    _write(p, {"skills": [
        {"name": "read", "repo": "tw93/Waza", "path": "skills/read"},
        {"name": "read", "repo": "tw93/Waza", "path": "skills/other"},
    ]})
    with pytest.raises(ConfigError, match="duplicate"):
        load_project_config(p)


def test_derived_sources_unique_order() -> None:
    cfg = ProjectConfig(skills=[
        SkillRef("a", "o1/r1", "p1"),
        SkillRef("b", "o2/r2", "p2"),
        SkillRef("c", "o1/r1", "p3"),
    ])
    assert derived_sources(cfg) == ["o1/r1", "o2/r2"]


def test_load_global_config_missing(tmp_path: Path) -> None:
    assert load_global_config(tmp_path / "config.json").sources == {}


def test_load_global_config_valid(tmp_path: Path) -> None:
    p = tmp_path / "config.json"
    _write(p, {"sources": {"tw93/Waza": {"commit": "abc", "url": "https://github.com/tw93/Waza.git"}}})
    cfg = load_global_config(p)
    assert cfg.sources["tw93/Waza"] == Source("tw93/Waza", "abc", "https://github.com/tw93/Waza.git")


def test_global_config_roundtrip(tmp_path: Path) -> None:
    p = tmp_path / "cfg" / "config.json"
    cfg = GlobalConfig(sources={"tw93/Waza": Source("tw93/Waza", "abc", "https://github.com/tw93/Waza.git")})
    save_global_config(p, cfg)
    assert load_global_config(p) == cfg
```

### Success Criteria:

#### Automated Verification:
- [x] `uv run pytest tests/test_config.py` 全部通过
- [x] `ruff check src/skill_manager/config.py tests/test_config.py` 无错误
- [x] `ruff format --check src/skill_manager/config.py tests/test_config.py` 无差异
- [x] 合法 `.skill-manager.json`（含 `path="."`）正确反序列化为 ProjectConfig
- [x] 坏 JSON / 非 object / 缺 skills / 缺 name / 缺 repo / 缺 path → 抛 ConfigError
- [x] name 含路径分隔符或 `.`/`..`/空 → 抛 ConfigError
- [x] repo 非 `owner/repo` 格式，或任一段不是安全 basename/GitHub slug（如 `../evil`、`owner/..`、反斜杠、空格）→ 抛 ConfigError
- [x] path 为绝对路径、含 `..`/`.`/空段/反斜杠/尾随斜杠 → 抛 ConfigError
- [x] 配置内重名 → 抛 ConfigError
- [x] `derived_sources` 去重且保持首次出现顺序
- [x] 全局配置缺失文件 → 空 GlobalConfig；sources dict 读写往返一致（自动建父目录）

#### Manual Verification:
- [ ] 校验错误信息含具体字段与原因（如 `skill 'read' repo 'tw93' must be 'owner/repo' in <path>`），便于定位

---

## Phase 3: Sources/git 层

### Overview

实现 `ensure_source`：对每个声明 repo，缺失则 clone 到全局缓存、已存在则 `git pull --ff-only`（让 HEAD 随上游前进，比 bare fetch 更正确），并记录 HEAD 到全局配置 sources dict（原地更新，由调用方统一保存）。clone/pull 经 subprocess（list 形参、不开 shell）。conftest.py 引入 `file://` 源仓库工厂 + XDG 隔离，供本 phase 与 Phase 5 复用。path 存在性校验移至 Phase 4 links 层；sources 不重定义 `repo_cache_path`（取 `cache_root` 参数，由调用方传 `paths.repos_cache_dir()`）。依赖 Phase 2 的 config 类型与全局配置读写。

### Changes Required:

#### 1. src/skill_manager/sources.py
**File**: src/skill_manager/sources.py
**Changes**: NEW — `ensure_source`（clone/`pull --ff-only` + HEAD + 全局配置原地更新）、`repo_url` 推导、`SourceError`、subprocess git 调用封装（list 形参、不开 shell）；不重定义 `repo_cache_path`（取 `cache_root` 参数）

```python
"""Source repository git operations for skill-manager.

ensure_source clones/pulls a repo into the cache and records its HEAD in the
global config. All git calls use subprocess with list arguments (no shell).
"""

from __future__ import annotations

import subprocess
from pathlib import Path

from skill_manager.config import GlobalConfig, Source


class SourceError(Exception):
    """Raised when a git operation on a source repo fails."""


def repo_url(repo: str) -> str:
    """Derive the HTTPS clone URL for an ``owner/repo`` identifier."""
    return f"https://github.com/{repo}.git"


def _run_git(args: list[str], cwd: Path | None = None) -> str:
    """Run git with list args (no shell). Return stdout. Raise SourceError on failure."""
    result = subprocess.run(["git", *args], cwd=cwd, capture_output=True, text=True, check=False)
    if result.returncode != 0:
        cmd = " ".join(args)
        raise SourceError(f"git {cmd} failed (exit {result.returncode}): {result.stderr.strip()}")
    return result.stdout.strip()


def ensure_source(
    repo: str,
    global_config: GlobalConfig,
    cache_root: Path,
    *,
    url: str | None = None,
) -> str:
    """Clone repo into cache if missing, else pull --ff-only. Record HEAD+url in global_config.

    Returns the current HEAD commit sha. ``url`` defaults to ``repo_url(repo)``;
    tests pass a ``file://`` URL to use a local repo as an offline GitHub stand-in.
    """
    actual_url = url if url is not None else repo_url(repo)
    dest = cache_root / repo
    if not dest.exists():
        dest.parent.mkdir(parents=True, exist_ok=True)
        _run_git(["clone", "--quiet", actual_url, str(dest)])
    else:
        # pull --ff-only (not bare fetch) so HEAD advances when upstream moves;
        # the cache is a pure mirror with no local commits, so ff always succeeds.
        _run_git(["pull", "--quiet", "--ff-only"], cwd=dest)
    head = _run_git(["rev-parse", "HEAD"], cwd=dest)
    global_config.sources[repo] = Source(repo=repo, commit=head, url=actual_url)
    return head
```

#### 2. tests/conftest.py
**File**: tests/conftest.py
**Changes**: NEW — 共享 fixtures：`isolated_xdg`（autouse，重定向 XDG 到 tmp）、`make_source_repo` 工厂（`git init` + 提交含 SKILL.md 的技能目录，返回源仓库 Path）、`git` helper（供测试向源仓库追加提交模拟上游前进）；`tmp_project` 延至 Phase 5 加入

```python
"""Shared pytest fixtures for skill-manager tests.

Provides a file:// source-repo factory (offline GitHub stand-in) and XDG
isolation so tests never touch the real ~/.config or ~/.cache.
"""

from __future__ import annotations

import subprocess
from pathlib import Path

import pytest


@pytest.fixture(autouse=True)
def isolated_xdg(tmp_path: Path, monkeypatch: pytest.MonkeyPatch) -> None:
    """Redirect XDG dirs to tmp so tests never touch real ~/.config or ~/.cache."""
    monkeypatch.setenv("XDG_CONFIG_HOME", str(tmp_path / "config"))
    monkeypatch.setenv("XDG_CACHE_HOME", str(tmp_path / "cache"))


def _git(args: list[str], cwd: Path) -> None:
    subprocess.run(["git", *args], cwd=cwd, check=True, capture_output=True)


@pytest.fixture
def make_source_repo(tmp_path: Path):
    """Factory: create a cloneable source repo with skill dirs containing SKILL.md.

    ``skills`` maps skill path -> SKILL.md content; path ``"."`` places SKILL.md
    at the repo root (repo-root skill case). Returns the source repo Path; tests
    build the ``file://`` URL and may add commits to simulate upstream advances.
    """

    def _make(name: str, skills: dict[str, str]) -> Path:
        repo = tmp_path / "sources" / name
        repo.mkdir(parents=True)
        _git(["init"], repo)
        _git(["config", "user.email", "test@example.com"], repo)
        _git(["config", "user.name", "Test"], repo)
        for skill_path, content in skills.items():
            skill_dir = repo if skill_path == "." else repo / skill_path
            skill_dir.mkdir(parents=True, exist_ok=True)
            (skill_dir / "SKILL.md").write_text(content, encoding="utf-8")
        _git(["add", "."], repo)
        _git(["commit", "-m", "init"], repo)
        return repo

    return _make


@pytest.fixture
def git():
    """Helper to run git in a given cwd (for tests that advance upstream)."""

    def _git(args: list[str], cwd: Path) -> None:
        subprocess.run(["git", *args], cwd=cwd, check=True, capture_output=True)

    return _git
```

#### 3. tests/test_sources.py
**File**: tests/test_sources.py
**Changes**: NEW — ensure_source 单测：repo_url 推导、缺失 clone（断言 `.git`/HEAD/sources 记录）、幂等（两次 HEAD 相同）、已存在走 pull 分支不报错、根目录技能（`path="."`）、上游前进后 HEAD 更新（追加 commit 再 ensure_source）、git 失败抛 SourceError

```python
import subprocess
from pathlib import Path

import pytest

from skill_manager.config import GlobalConfig
from skill_manager.sources import SourceError, ensure_source, repo_url


def test_repo_url() -> None:
    assert repo_url("tw93/Waza") == "https://github.com/tw93/Waza.git"


def test_ensure_source_clones_missing(tmp_path: Path, make_source_repo) -> None:
    repo = make_source_repo("waza", {"skills/read": "# read\n"})
    url = f"file://{repo}"
    cache = tmp_path / "cache" / "repos"
    cfg = GlobalConfig()
    head = ensure_source("tw93/Waza", cfg, cache, url=url)
    assert (cache / "tw93" / "Waza" / ".git").exists()
    assert len(head) == 40 and all(c in "0123456789abcdef" for c in head)
    assert cfg.sources["tw93/Waza"].commit == head
    assert cfg.sources["tw93/Waza"].url == url


def test_ensure_source_idempotent(tmp_path: Path, make_source_repo) -> None:
    repo = make_source_repo("waza", {"skills/read": "# read\n"})
    url = f"file://{repo}"
    cache = tmp_path / "cache" / "repos"
    cfg = GlobalConfig()
    head1 = ensure_source("tw93/Waza", cfg, cache, url=url)
    head2 = ensure_source("tw93/Waza", cfg, cache, url=url)
    assert head1 == head2


def test_ensure_source_pulls_existing(tmp_path: Path, make_source_repo) -> None:
    repo = make_source_repo("waza", {"skills/read": "# read\n"})
    url = f"file://{repo}"
    cache = tmp_path / "cache" / "repos"
    cfg = GlobalConfig()
    ensure_source("tw93/Waza", cfg, cache, url=url)
    # second call takes the pull branch (dest exists); must not raise
    head = ensure_source("tw93/Waza", cfg, cache, url=url)
    assert cfg.sources["tw93/Waza"].commit == head


def test_ensure_source_root_path(tmp_path: Path, make_source_repo) -> None:
    repo = make_source_repo("kami", {".": "# kami\n"})
    url = f"file://{repo}"
    cache = tmp_path / "cache" / "repos"
    cfg = GlobalConfig()
    ensure_source("tw93/Kami", cfg, cache, url=url)
    assert (cache / "tw93" / "Kami" / "SKILL.md").is_file()


def test_ensure_source_updates_head_after_upstream_advance(
    tmp_path: Path, make_source_repo, git
) -> None:
    repo = make_source_repo("waza", {"skills/read": "# read\n"})
    url = f"file://{repo}"
    cache = tmp_path / "cache" / "repos"
    cfg = GlobalConfig()
    head1 = ensure_source("tw93/Waza", cfg, cache, url=url)
    # advance upstream
    (repo / "skills" / "read" / "extra.txt").write_text("x", encoding="utf-8")
    git(["add", "."], repo)
    git(["commit", "-m", "advance"], repo)
    head2 = ensure_source("tw93/Waza", cfg, cache, url=url)
    assert head2 != head1
    assert cfg.sources["tw93/Waza"].commit == head2


def test_ensure_source_git_failure(tmp_path: Path) -> None:
    cache = tmp_path / "cache" / "repos"
    cfg = GlobalConfig()
    with pytest.raises(SourceError, match="clone"):
        ensure_source("x/y", cfg, cache, url="file:///nonexistent/repo")
```

### Success Criteria:

#### Automated Verification:
- [x] `uv run pytest tests/test_sources.py` 全部通过（离线，file:// 源仓库）
- [x] `ruff check src/skill_manager/sources.py tests/conftest.py tests/test_sources.py` 无错误
- [x] `ruff format --check src/skill_manager/sources.py tests/conftest.py tests/test_sources.py` 无差异
- [x] `repo_url("owner/repo")` == `https://github.com/owner/repo.git`
- [x] 首次 ensure_source：仓库 clone 到缓存目录（`.git` 存在），全局配置 sources[repo].commit = 当前 HEAD（40 位 hex）
- [x] 再次 ensure_source：`pull --ff-only`（不重复 clone），幂等无报错
- [x] 上游前进后 ensure_source：HEAD 随 `pull --ff-only` 前进，sources[repo].commit 更新
- [x] 根目录技能（`path="."`）：clone 后 `<cache>/<repo>/SKILL.md` 存在
- [x] git 失败（如不存在的 file:// url）→ 抛 SourceError
- [x] git subprocess 调用用 list 形参、未开 shell（ruff S603/S607 已在 pyproject 忽略）

#### Manual Verification:
- [ ] 全局配置缺省目录不存在时首次写入自动创建（由调用方 `save_global_config` 负责）
- [ ] `config.json` 的 sources 为 dict，commit 字段为 40 字符 commit hash
- [ ] path 在仓库中不存在的校验由 Phase 4 links 层负责（`<repo>/<path>` 非目录 → LinkError）

---

## Phase 4: Links 层

### Overview

实现 `ensure_link`：在项目 `./.agents/skills/` 下为技能创建 symlink 指向 `<repo_cache>/<repo>/<path>` 目录（绝对路径目标）。承接 Phase 3 移入的 path 存在性与 `SKILL.md` 校验，并二次确认 resolved target 仍位于 resolved repo root 内，防止直接调用层绕过 config 校验造成路径穿越。已存在且指向相同目标 → 幂等 `exists`；已存在指向他处（外部 symlink）或为非 symlink 真实条目 → 跳过不覆盖（`skipped`）；目标 path 非目录或无 `SKILL.md` → `LinkError`。依赖 Phase 2 的 `SkillRef` 类型；`cache_root` 由调用方传入，不 import paths。

### Changes Required:

#### 1. src/skill_manager/links.py
**File**: src/skill_manager/links.py
**Changes**: NEW — `ensure_link`（创建/校验/幂等/安全跳过 + repo 边界防御）、`LinkResult`（name/action/target）、`LinkError`、可复用 `link_points_to`；symlink 目标用绝对路径

```python
"""Symlink creation for project skills.

ensure_link creates a symlink in the project's ``.agents/skills/`` directory
pointing at a cloned skill's directory in the source cache. It never overwrites
an existing non-tool entry (safety default per FRD).
"""

from __future__ import annotations

import os
from dataclasses import dataclass
from pathlib import Path

from skill_manager.config import SkillRef


class LinkError(Exception):
    """Raised when a skill's source path is missing or has no SKILL.md."""


@dataclass(frozen=True)
class LinkResult:
    name: str
    action: str  # "created" | "exists" | "skipped"
    target: Path


def link_points_to(link: Path, target: Path) -> bool:
    """True if ``link`` is a symlink whose stored target resolves to ``target``."""
    if not link.is_symlink():
        return False
    # os.readlink gives the stored (possibly relative) path; resolve against link's dir
    stored = os.readlink(link)
    resolved = (link.parent / stored).resolve() if not Path(stored).is_absolute() else Path(stored)
    return resolved == target.resolve()


def ensure_link(skill: SkillRef, cache_root: Path, skills_dir: Path) -> LinkResult:
    """Create or verify a symlink for ``skill`` under ``skills_dir``.

    Target is the absolute ``cache_root/<repo>/<path>`` directory. Returns a
    LinkResult: ``created`` (new), ``exists`` (already points to target), or
    ``skipped`` (occupied by an external symlink or non-symlink entry).
    """
    repo_root = (cache_root / skill.repo).resolve()
    target = (repo_root / skill.path).resolve()
    if not target.is_relative_to(repo_root):
        raise LinkError(f"skill {skill.name!r} path {skill.path!r} escapes repo {skill.repo}")
    if not target.is_dir():
        raise LinkError(f"skill {skill.name!r} path {skill.path!r} not found in {skill.repo}")
    if not (target / "SKILL.md").is_file():
        raise LinkError(f"skill {skill.name!r}: no SKILL.md at {target}")

    link = skills_dir / skill.name
    if link.is_symlink():
        if link_points_to(link, target):
            return LinkResult(skill.name, "exists", target)
        # external symlink -> do not overwrite (safety default)
        return LinkResult(skill.name, "skipped", target)
    if link.exists():
        # non-symlink real entry (dir/file) -> do not overwrite
        return LinkResult(skill.name, "skipped", target)

    skills_dir.mkdir(parents=True, exist_ok=True)
    os.symlink(target, link)  # absolute target: survives skills_dir relocation
    return LinkResult(skill.name, "created", target)
```

#### 2. tests/test_links.py
**File**: tests/test_links.py
**Changes**: NEW — ensure_link 单测：新建 symlink（断言有效、SKILL.md 可读）、幂等 `exists`、外部 symlink 跳过不覆盖、非 symlink 真实目录跳过、path 不存在 → LinkError、无 SKILL.md → LinkError、skills_dir 不存在自动建、根目录技能 `path="."` 链向 repo 根；直接造缓存目录结构，无需 git

```python
from pathlib import Path

import pytest

from skill_manager.config import SkillRef
from skill_manager.links import LinkError, ensure_link


def _make_skill(cache: Path, repo: str, path: str, content: str = "# skill\n") -> Path:
    """Lay out a cached skill dir with SKILL.md; return its resolved path."""
    target = cache / repo / path
    target.mkdir(parents=True, exist_ok=True)
    (target / "SKILL.md").write_text(content, encoding="utf-8")
    return target.resolve()


def test_ensure_link_creates(tmp_path: Path) -> None:
    cache = tmp_path / "cache" / "repos"
    _make_skill(cache, "tw93/Waza", "skills/read")
    skills_dir = tmp_path / "proj" / ".agents" / "skills"
    res = ensure_link(SkillRef("read", "tw93/Waza", "skills/read"), cache, skills_dir)
    assert res.action == "created"
    link = skills_dir / "read"
    assert link.is_symlink()
    assert (link / "SKILL.md").read_text() == "# skill\n"


def test_ensure_link_idempotent(tmp_path: Path) -> None:
    cache = tmp_path / "cache" / "repos"
    _make_skill(cache, "tw93/Waza", "skills/read")
    skills_dir = tmp_path / "proj" / ".agents" / "skills"
    ensure_link(SkillRef("read", "tw93/Waza", "skills/read"), cache, skills_dir)
    res = ensure_link(SkillRef("read", "tw93/Waza", "skills/read"), cache, skills_dir)
    assert res.action == "exists"


def test_ensure_link_skips_external_symlink(tmp_path: Path) -> None:
    cache = tmp_path / "cache" / "repos"
    _make_skill(cache, "tw93/Waza", "skills/read")
    skills_dir = tmp_path / "proj" / ".agents" / "skills"
    skills_dir.mkdir(parents=True)
    elsewhere = tmp_path / "elsewhere"
    elsewhere.mkdir()
    (elsewhere / "SKILL.md").write_text("# other\n", encoding="utf-8")
    (skills_dir / "read").symlink_to(elsewhere)
    res = ensure_link(SkillRef("read", "tw93/Waza", "skills/read"), cache, skills_dir)
    assert res.action == "skipped"
    # unchanged: still points elsewhere
    assert (skills_dir / "read").readlink() == elsewhere


def test_ensure_link_skips_real_directory(tmp_path: Path) -> None:
    cache = tmp_path / "cache" / "repos"
    _make_skill(cache, "tw93/Waza", "skills/read")
    skills_dir = tmp_path / "proj" / ".agents" / "skills"
    (skills_dir / "read").mkdir(parents=True)
    res = ensure_link(SkillRef("read", "tw93/Waza", "skills/read"), cache, skills_dir)
    assert res.action == "skipped"
    assert not (skills_dir / "read").is_symlink()


def test_ensure_link_path_not_found(tmp_path: Path) -> None:
    cache = tmp_path / "cache" / "repos"
    cache.mkdir(parents=True)
    skills_dir = tmp_path / "proj" / ".agents" / "skills"
    with pytest.raises(LinkError, match="not found"):
        ensure_link(SkillRef("read", "tw93/Waza", "skills/missing"), cache, skills_dir)


def test_ensure_link_rejects_repo_escape(tmp_path: Path) -> None:
    cache = tmp_path / "cache" / "repos"
    repo_root = cache / "tw93" / "Waza"
    outside = (repo_root / "../../outside").resolve()
    outside.mkdir(parents=True)
    (outside / "SKILL.md").write_text("# outside\n", encoding="utf-8")
    skills_dir = tmp_path / "proj" / ".agents" / "skills"
    with pytest.raises(LinkError, match="escapes"):
        ensure_link(SkillRef("read", "tw93/Waza", "../../outside"), cache, skills_dir)


def test_ensure_link_no_skill_md(tmp_path: Path) -> None:
    cache = tmp_path / "cache" / "repos"
    (cache / "tw93/Waza" / "skills/read").mkdir(parents=True)
    skills_dir = tmp_path / "proj" / ".agents" / "skills"
    with pytest.raises(LinkError, match="SKILL.md"):
        ensure_link(SkillRef("read", "tw93/Waza", "skills/read"), cache, skills_dir)


def test_ensure_link_creates_skills_dir(tmp_path: Path) -> None:
    cache = tmp_path / "cache" / "repos"
    _make_skill(cache, "tw93/Waza", "skills/read")
    skills_dir = tmp_path / "proj" / ".agents" / "skills"
    assert not skills_dir.exists()
    ensure_link(SkillRef("read", "tw93/Waza", "skills/read"), cache, skills_dir)
    assert skills_dir.is_dir()


def test_ensure_link_root_path(tmp_path: Path) -> None:
    cache = tmp_path / "cache" / "repos"
    target = _make_skill(cache, "tw93/Kami", ".")
    skills_dir = tmp_path / "proj" / ".agents" / "skills"
    res = ensure_link(SkillRef("kami", "tw93/Kami", "."), cache, skills_dir)
    assert res.action == "created"
    assert (skills_dir / "kami").is_symlink()
    assert (skills_dir / "kami" / "SKILL.md").is_file()
```

### Success Criteria:

#### Automated Verification:
- [x] `uv run pytest tests/test_links.py` 全部通过
- [x] `ruff check src/skill_manager/links.py tests/test_links.py` 无错误
- [x] `ruff format --check src/skill_manager/links.py tests/test_links.py` 无差异
- [x] 首次 ensure_link：`skills_dir/<name>` 为有效 symlink，指向 `<cache_root>/<repo>/<path>`，`<path>/SKILL.md` 可读
- [x] 重复 ensure_link：幂等 no-op，返回 action=`exists`
- [x] 已存在指向他处的 symlink：跳过、不覆盖、返回 action=`skipped`
- [x] 已存在非 symlink 真实目录/文件：跳过、不覆盖、返回 action=`skipped`
- [x] 目标 path 在缓存中非目录 → 抛 `LinkError`（match="not found"）
- [x] 即使直接构造 `SkillRef(path="../../outside")` 绕过 config，resolved target 逃出 repo root 时也抛 `LinkError`（match="escapes"）且不创建 symlink
- [x] 目标目录无 `SKILL.md` → 抛 `LinkError`（match="SKILL.md"）
- [x] `skills_dir` 不存在时首次创建自动建父目录
- [x] 根目录技能（`path="."`）：symlink 指向 `<cache_root>/<repo>`，`SKILL.md` 在 repo 根

#### Manual Verification:
- [ ] `ls -l ./.agents/skills/` 显示有效 symlink，`cat ./.agents/skills/<name>/SKILL.md` 可读

---

## Phase 5: CLI + 集成

### Overview

用 typer 串联 `sync` 与 `list` 子命令。可测编排核心 `run_sync`/`run_list` 与薄命令包装分离：sync = 读项目配置 → 派生 sources → ensure_source 每个 repo → save_global_config → ensure_link 每个技能 → 汇总输出；list = 读项目配置 + 全局配置 → 报告 sources 与技能状态（仓库目录是否存在、symlink 状态：`linked`/`external`/`broken`/`unlinked`，外部 symlink 不得误报为 linked）。`url_resolver` 钩子让测试注入 `file://` URL（生产默认 `sources.repo_url`），不需网络。命令捕获 `(ConfigError, SourceError, LinkError)` → `typer.Exit(1)`。端到端集成测试覆盖幂等、错误退出码、list 输出。README 补充用法文档。依赖 Phase 2/3/4。

### Changes Required:

#### 1. src/skill_manager/cli.py
**File**: src/skill_manager/cli.py
**Changes**: MODIFY — 在 Phase 1 桩基础上加入 imports + `run_sync`/`run_list` 编排核心 + `sync`/`list_` 薄命令包装；保留 `app`/`main`/`__main__`。下为完整新文件内容（桩仅 ~15 行，大部分为新代码，全文件最可实施）

```python
"""skill-manager CLI entry point.

Provides the Typer app, sync/list commands, and console_scripts target.
"""

from __future__ import annotations

from collections.abc import Callable
from pathlib import Path

import typer

from skill_manager import config, links, paths, sources
from skill_manager.config import ConfigError
from skill_manager.links import LinkError
from skill_manager.sources import SourceError

app = typer.Typer(
    name="skill-manager",
    help="Project-scoped declarative skill manager for pi agent skills.",
    no_args_is_help=True,
)


def run_sync(
    project_config: Path,
    global_config_path: Path,
    cache_root: Path,
    skills_dir: Path,
    *,
    url_resolver: Callable[[str], str] | None = None,
) -> None:
    """Orchestrate sync: ensure sources, record HEADs, link skills.

    ``url_resolver`` defaults to ``sources.repo_url`` (GitHub HTTPS); tests pass
    a ``file://`` resolver to use local repos as offline GitHub stand-ins.
    """
    resolver = url_resolver or sources.repo_url
    proj = config.load_project_config(project_config)
    repos = config.derived_sources(proj)
    global_cfg = config.load_global_config(global_config_path)
    for repo in repos:
        head = sources.ensure_source(repo, global_cfg, cache_root, url=resolver(repo))
        typer.echo(f"ensured {repo} ({head[:8]})")
    config.save_global_config(global_config_path, global_cfg)
    for skill in proj.skills:
        result = links.ensure_link(skill, cache_root, skills_dir)
        typer.echo(f"{result.action} {skill.name} -> {result.target}")


def run_list(
    project_config: Path,
    global_config_path: Path,
    cache_root: Path,
    skills_dir: Path,
) -> None:
    """List declared sources and enabled skills with status."""
    proj = config.load_project_config(project_config)
    global_cfg = config.load_global_config(global_config_path)
    typer.echo("Sources:")
    for repo in config.derived_sources(proj):
        cached = (cache_root / repo).is_dir()
        src = global_cfg.sources.get(repo)
        head = src.commit[:8] if src else "-"
        typer.echo(f"  {repo}  {head}  {'cached' if cached else 'missing'}")
    typer.echo("Skills:")
    for skill in proj.skills:
        link = skills_dir / skill.name
        target_path = (cache_root / skill.repo / skill.path).resolve()
        repo_present = target_path.is_dir()
        if link.is_symlink():
            points_to_declared = links.link_points_to(link, target_path)
            if points_to_declared and link.exists():
                link_status = "linked"
            elif points_to_declared:
                link_status = "broken"
            elif link.exists():
                link_status = "external"
            else:
                link_status = "broken"
        else:
            link_status = "unlinked"
        typer.echo(
            f"  {skill.name}  {skill.repo}:{skill.path}  "
            f"{link_status}  {'present' if repo_present else 'absent'}"
        )


@app.command()
def sync() -> None:
    """Sync declared skills into ./.agents/skills/."""
    try:
        run_sync(
            paths.project_config_path(),
            paths.config_file(),
            paths.repos_cache_dir(),
            paths.project_skills_dir(),
        )
    except (ConfigError, SourceError, LinkError) as e:
        typer.echo(f"Error: {e}", err=True)
        raise typer.Exit(code=1) from e


@app.command(name="list")
def list_() -> None:
    """List declared sources and skills with status."""
    try:
        run_list(
            paths.project_config_path(),
            paths.config_file(),
            paths.repos_cache_dir(),
            paths.project_skills_dir(),
        )
    except (ConfigError, SourceError, LinkError) as e:
        typer.echo(f"Error: {e}", err=True)
        raise typer.Exit(code=1) from e


def main() -> None:
    """Entry point for ``python -m skill_manager``."""
    app()


if __name__ == "__main__":
    main()
```

#### 2. tests/test_cli.py
**File**: tests/test_cli.py
**Changes**: NEW — 端到端集成：`sync --help`/`list --help` 退出 0；`sync` 缺配置→退出 1 + "not found"；`sync` 坏 JSON→退出 1 + "invalid JSON"；`run_sync` 端到端（url_resolver 注入 file:// → clone+symlink+全局配置记 HEAD）；`run_sync` 幂等（两次无异常）；`run_sync` path 不存在→LinkError；`run_list`（capsys 断言输出含 repo/skill/linked）；外部 symlink 下 `run_list` 输出 `external` 而不是 `linked`；缓存目标被删后 `run_list` 输出 `broken`。命令级用 CliRunner，编排级直接调 run_sync/run_list + url_resolver

```python
import json
import shutil
from pathlib import Path

import pytest
from typer.testing import CliRunner

from skill_manager.cli import app, run_list, run_sync
from skill_manager.links import LinkError

runner = CliRunner()


def _write_config(project: Path, skills: list[dict]) -> None:
    (project / ".skill-manager.json").write_text(
        json.dumps({"skills": skills}), encoding="utf-8"
    )


def test_sync_help() -> None:
    result = runner.invoke(app, ["sync", "--help"])
    assert result.exit_code == 0
    assert "sync" in result.stdout.lower()


def test_list_help() -> None:
    result = runner.invoke(app, ["list", "--help"])
    assert result.exit_code == 0
    assert "list" in result.stdout.lower()


def test_sync_missing_config(tmp_path: Path, monkeypatch: pytest.MonkeyPatch) -> None:
    monkeypatch.chdir(tmp_path)
    result = runner.invoke(app, ["sync"])
    assert result.exit_code == 1
    assert "not found" in result.output


def test_sync_bad_json(tmp_path: Path, monkeypatch: pytest.MonkeyPatch) -> None:
    monkeypatch.chdir(tmp_path)
    (tmp_path / ".skill-manager.json").write_text("{bad", encoding="utf-8")
    result = runner.invoke(app, ["sync"])
    assert result.exit_code == 1
    assert "invalid JSON" in result.output


def test_run_sync_end_to_end(tmp_path: Path, make_source_repo) -> None:
    repo = make_source_repo("waza", {"skills/read": "# read\n"})
    url = f"file://{repo}"
    project = tmp_path / "proj"
    project.mkdir()
    _write_config(project, [{"name": "read", "repo": "tw93/Waza", "path": "skills/read"}])
    cache = tmp_path / "repos"
    gconfig = tmp_path / "config.json"
    skills_dir = project / ".agents" / "skills"
    run_sync(project / ".skill-manager.json", gconfig, cache, skills_dir, url_resolver=lambda r: url)
    assert (skills_dir / "read").is_symlink()
    assert (skills_dir / "read" / "SKILL.md").read_text() == "# read\n"
    gdata = json.loads(gconfig.read_text())
    assert "tw93/Waza" in gdata["sources"]
    assert len(gdata["sources"]["tw93/Waza"]["commit"]) == 40


def test_run_sync_idempotent(tmp_path: Path, make_source_repo) -> None:
    repo = make_source_repo("waza", {"skills/read": "# read\n"})
    url = f"file://{repo}"
    project = tmp_path / "proj"
    project.mkdir()
    _write_config(project, [{"name": "read", "repo": "tw93/Waza", "path": "skills/read"}])
    cache = tmp_path / "repos"
    gconfig = tmp_path / "config.json"
    skills_dir = project / ".agents" / "skills"
    run_sync(project / ".skill-manager.json", gconfig, cache, skills_dir, url_resolver=lambda r: url)
    run_sync(project / ".skill-manager.json", gconfig, cache, skills_dir, url_resolver=lambda r: url)
    assert (skills_dir / "read").is_symlink()


def test_run_sync_path_not_found(tmp_path: Path, make_source_repo) -> None:
    repo = make_source_repo("waza", {"skills/read": "# read\n"})
    url = f"file://{repo}"
    project = tmp_path / "proj"
    project.mkdir()
    _write_config(project, [{"name": "read", "repo": "tw93/Waza", "path": "skills/missing"}])
    cache = tmp_path / "repos"
    gconfig = tmp_path / "config.json"
    skills_dir = project / ".agents" / "skills"
    with pytest.raises(LinkError, match="not found"):
        run_sync(project / ".skill-manager.json", gconfig, cache, skills_dir, url_resolver=lambda r: url)


def test_run_list(tmp_path: Path, make_source_repo, capsys: pytest.CaptureFixture[str]) -> None:
    repo = make_source_repo("waza", {"skills/read": "# read\n"})
    url = f"file://{repo}"
    project = tmp_path / "proj"
    project.mkdir()
    _write_config(project, [{"name": "read", "repo": "tw93/Waza", "path": "skills/read"}])
    cache = tmp_path / "repos"
    gconfig = tmp_path / "config.json"
    skills_dir = project / ".agents" / "skills"
    run_sync(project / ".skill-manager.json", gconfig, cache, skills_dir, url_resolver=lambda r: url)
    run_list(project / ".skill-manager.json", gconfig, cache, skills_dir)
    captured = capsys.readouterr()
    assert "tw93/Waza" in captured.out
    assert "read" in captured.out
    assert "linked" in captured.out


def test_run_list_external_symlink(
    tmp_path: Path,
    make_source_repo,
    capsys: pytest.CaptureFixture[str],
) -> None:
    repo = make_source_repo("waza", {"skills/read": "# read\n"})
    url = f"file://{repo}"
    project = tmp_path / "proj"
    project.mkdir()
    _write_config(project, [{"name": "read", "repo": "tw93/Waza", "path": "skills/read"}])
    cache = tmp_path / "repos"
    gconfig = tmp_path / "config.json"
    skills_dir = project / ".agents" / "skills"
    skills_dir.mkdir(parents=True)
    elsewhere = tmp_path / "elsewhere"
    elsewhere.mkdir()
    (elsewhere / "SKILL.md").write_text("# other\n", encoding="utf-8")
    (skills_dir / "read").symlink_to(elsewhere)
    run_sync(project / ".skill-manager.json", gconfig, cache, skills_dir, url_resolver=lambda r: url)
    run_list(project / ".skill-manager.json", gconfig, cache, skills_dir)
    captured = capsys.readouterr()
    assert "read  tw93/Waza:skills/read  external" in captured.out


def test_run_list_broken(
    tmp_path: Path, make_source_repo, capsys: pytest.CaptureFixture[str]
) -> None:
    repo = make_source_repo("waza", {"skills/read": "# read\n"})
    url = f"file://{repo}"
    project = tmp_path / "proj"
    project.mkdir()
    _write_config(project, [{"name": "read", "repo": "tw93/Waza", "path": "skills/read"}])
    cache = tmp_path / "repos"
    gconfig = tmp_path / "config.json"
    skills_dir = project / ".agents" / "skills"
    run_sync(project / ".skill-manager.json", gconfig, cache, skills_dir, url_resolver=lambda r: url)
    # remove the cached skill target -> symlink now dangles but still points at the declared path
    shutil.rmtree(cache / "tw93" / "Waza" / "skills" / "read")
    run_list(project / ".skill-manager.json", gconfig, cache, skills_dir)
    captured = capsys.readouterr()
    assert "read  tw93/Waza:skills/read  broken" in captured.out
```

#### 3. README.md
**File**: README.md
**Changes**: MODIFY — 原文件仅一行 `# Skill Manager`，此为完整新内容：工具简介、安装（uv）、`.skill-manager.json` 配置格式（name/repo/path 三字段说明，含 `path="."` 根目录技能）、sync/list 用法、幂等与不覆盖说明、XDG 路径表

````markdown
# Skill Manager

Project-scoped declarative skill manager for [pi](https://github.com/earendil-works/pi-coding-agent) agent skills.

Keep global skills minimal; declare per-project which skills to enable. `skill-manager` clones GitHub skill source repos into an XDG cache and symlinks selected skills into your project's `./.agents/skills/`.

## Install

```bash
uv tool install .  # or run in-place: uv run skill-manager
```

## Configure

Create `./.skill-manager.json` in your project:

```json
{
  "skills": [
    {"name": "read", "repo": "tw93/Waza", "path": "skills/read"},
    {"name": "kami", "repo": "tw93/Kami", "path": "."}
  ]
}
```

- `name`: symlink name under `./.agents/skills/` (must be a single path component; duplicates error)
- `repo`: GitHub `owner/repo` (both parts must be safe slug components)
- `path`: skill directory inside the repo (`.` for repo-root skills; must contain `SKILL.md`; path traversal is rejected)

## Usage

```bash
skill-manager sync   # clone/fetch sources, link declared skills
skill-manager list   # show sources and skill status
```

`sync` is idempotent and never overwrites an existing non-tool symlink (it skips with a notice). Sources are derived from the declared skills' `repo` fields.

## Paths (XDG)

| What | Path |
|------|------|
| Global config | `$XDG_CONFIG_HOME/skill-manager/config.json` (default `~/.config/skill-manager/config.json`) |
| Source cache | `$XDG_CACHE_HOME/skill-manager/repos/` (default `~/.cache/skill-manager/repos/`) |
| Project skills | `./.agents/skills/` |
````

### Success Criteria:

#### Automated Verification:
- [x] `uv run pytest` 全部通过（含 test_cli 端到端）
- [x] `uv run pytest -m "not network"` 全部通过（离线默认）
- [x] `ruff check .` 与 `ruff format --check .` 全绿
- [x] `pre-commit run --all-files` 通过
- [x] `uv run skill-manager sync` 在含合法 `.skill-manager.json` 项目中：clone+symlink 创建，退出 0
- [x] 再次 `uv run skill-manager sync`：无副作用，退出 0（幂等）
- [x] `uv run skill-manager list`：输出 sources 与技能状态，且能区分 `linked`/`external`/`broken`/`unlinked`
- [x] 坏 JSON 时 `uv run skill-manager sync` 退出非 0
- [x] path 不存在时 `uv run skill-manager sync` 退出非 0
- [x] 外部 symlink 存在时 sync 跳过不覆盖
- [x] 外部 symlink 存在时 `list` 输出 `external`，不误报为 `linked`

#### Manual Verification:
- [ ] `ls -l ./.agents/skills/` 每个技能为有效 symlink
- [ ] README 用法示例与实际命令输出一致
